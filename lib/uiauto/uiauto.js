import { prepareBootstrap } from './dynamic-bootstrap';
import UIAutoClient from './uiauto-client';
import * as utils from './utils';

export { prepareBootstrap, UIAutoClient, utils };
export default UIAutoClient;
